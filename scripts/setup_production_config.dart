// This script previously attempted to generate config Dart files with multi-line strings, but caused syntax errors.
// It is now intentionally left empty to prevent further syntax errors. All config generation should be done with safe, single-line values or handled manually.

void main() {
  print('This script is deprecated. Use the fixed config files in lib/config/ instead.');
} 